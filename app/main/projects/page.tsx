import { PageContainer } from "@/components/ui/container";
import { DataManage } from "@/modules/projects/list/DataManage";

const Page = () => {
  return (
    <PageContainer>
      <DataManage />
    </PageContainer>
  );
};

export default Page;
